<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Ocorrências Liberadas – Acesso Externo</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex h-screen bg-gray-100">

  <!-- Sidebar (mesmo layout das demais telas) -->
 <%- include('../partials/sidebar_externo') %>

  <!-- Conteúdo principal -->
  <main class="max-w-screen p-4" class="flex-1 p-6 overflow-y-auto">
    <h1 class="text-2xl font-bold mb-6">Ocorrências Liberadas para seu Órgão</h1>

    <% if (ocorrencias.length === 0) { %>
      <p class="text-gray-600">Nenhuma ocorrência foi liberada para seu órgão até o momento.</p>
    <% } else { %>
      <div class="overflow-x-auto">
        <div class="overflow-x-auto"><table class="min-w-full bg-white shadow-md rounded-lg">
          <thead class="bg-gray-200 text-sm font-semibold">
            <tr>
              <th class="px-4 py-2 border">Protocolo</th>
              <th class="px-4 py-2 border">Data/Hora</th>
              <th class="px-4 py-2 border">Local</th>
              <th class="px-4 py-2 border">Natureza</th>
              <th class="px-4 py-2 border">Cidade</th>
              <th class="px-4 py-2 border">Ação</th>
            </tr>
          </thead>
          <tbody>
            <% ocorrencias.forEach(o => { %>
              <tr class="border-t text-sm text-gray-700">
                <td class="px-4 py-2 border"><%= o.protocolo %></td>
                <td class="px-4 py-2 border"><%= o.data_hora_fato %></td>
                <td class="px-4 py-2 border"><%= o.local %></td>
                <td class="px-4 py-2 border"><%= o.natureza %></td>
                <td class="px-4 py-2 border">
                  <% if (o.cidade_nome) { %><%= o.cidade_nome %><% } else { %>----<% } %>
                </td>
                <td class="px-4 py-2 border text-center">
                   <a href="/externo/detalhes-ocorrencia/<%= o.id %>" class="bg-blue-600 hover:bg-blue-700 text-white px-3 py-1 rounded text-sm"> Detalhes  </a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table></div>
      </div>
    <% } %>
  </main>
</body>
</html>
