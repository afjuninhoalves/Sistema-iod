<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title>Nova Solicitação – SistemaCIM</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex h-screen bg-gray-100">
  <!-- Sidebar exclusiva de usuário externo -->
  <%- include('../partials/sidebar_externo') %>

  <main class="max-w-screen p-4" class="flex-1 p-6 overflow-y-auto">
    <h1 class="text-2xl font-semibold mb-6">Nova Solicitação</h1>

    <% if (erro) { %>
      <div class="bg-red-100 text-red-700 px-4 py-2 mb-4 rounded"><%= erro %></div>
    <% } %>

    

    <form class="space-y-4" action="/externo/solicitacoes/nova" method="POST" enctype="multipart/form-data" class="space-y-4 max-w-2xl bg-white p-6 rounded shadow">

      <div>
        <label class="block text-sm font-medium mb-1">Título</label>
        <input class="w-full" type="text" name="titulo" class="w-full border border-gray-300 rounded px-3 py-2" required />
      </div>

      <div>
        <label class="block text-sm font-medium mb-1">Descrição</label>
        <textarea name="descricao" rows="6" class="w-full border border-gray-300 rounded px-3 py-2" required></textarea>
      </div>

      <div>
        <label class="block text-sm font-medium mb-1">Anexar Documento (PDF ou DOC)</label>
        <input class="w-full" type="file" name="anexo" accept=".pdf,.doc,.docx" class="w-full border border-gray-300 rounded px-3 py-2">
      </div>

      

      <div class="flex justify-end space-x-2">
        <a href="/externo/home" class="bg-gray-300 text-gray-800 px-4 py-2 rounded hover:bg-gray-400">Cancelar</a>
        <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700">Enviar Solicitação</button>
      </div>
    </form>
  </main>
</body>
</html>
