<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Ações Conjuntas - SistemaCIM</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex flex-col h-screen">



  <div class="flex flex-1 bg-gray-100 overflow-hidden">
    <%- include('../partials/sidebar') %>

    <main class="max-w-screen p-4" class="flex-1 overflow-y-auto p-6">
      <h1 class="text-2xl font-semibold mb-6">Ações Conjuntas Abertas</h1>
      <a href="/acoes-conjuntas/new"
         class="inline-flex items-center px-4 py-2 mb-4 bg-blue-600 text-white rounded hover:bg-blue-700">
        Nova Ação Conjunta
      </a>

      <div class="overflow-x-auto bg-white rounded shadow">
        <div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">ID</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Responsável</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Objetivo</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Detalhes</th>
	      <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Ações</th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <% acoes.forEach(a => { %>
            <tr>
              <td class="px-6 py-4 whitespace-nowrap"><%= a.id %></td>
              <td class="px-6 py-4 whitespace-nowrap"><%= a.cidade_responsavel %></td>
              <td class="px-6 py-4 whitespace-nowrap"><%= a.objetivo %></td>
              <td class="px-6 py-4 whitespace-nowrap">
                <a href="/acoes-conjuntas/<%= a.id %>" class="text-blue-600 hover:underline">Ver</a>
              </td>
 	      <td class="px-6 py-4 whitespace-nowrap">
                <a href="/acoes-conjuntas/<%= a.id %>/edit"class="text-blue-600 hover:underline">Editar</a>
              </td>


            </tr>
            <% }) %>
          </tbody>
        </table></div>
      </div>
    </main>
  </div>

</body>
</html>
